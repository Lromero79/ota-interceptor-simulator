name: ota_interceptor_simulator
description: Educational OTA Call, Text, and Media Interceptor Simulator

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.0.0
  telephony: ^0.5.0
  flutter_webrtc: ^0.10.0
  provider: ^6.0.0
  path_provider: ^2.0.0
  google_maps_flutter: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
